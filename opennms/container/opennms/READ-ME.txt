
# datacollection-config.xml

# snmp-graph.properties.d/

# poller-configuration.xml

# Custom MIBs

# podman-compose down -v; podman-compose up --build --no-cache -d
# podman logs -f opennms

# podman exec -it opennms ls -l /opt/opennms/share
# podman exec -it postgres-database psql -U mavennms
# podman exec -it postgres-db psql -U opennms -d opennms -c "\dt"